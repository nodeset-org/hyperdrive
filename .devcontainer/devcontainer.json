// For format details, see https://aka.ms/devcontainer.json
{
	"name": "Hyperdrive",
	"build": {
        "dockerfile": "Dockerfile"
    },

	// this isn't working? Docker isn't starting
	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker": {}
	},

	"remoteUser": "nodeop",	

	// Use a different mount and starting point inside the container
	"workspaceMount": "source=${localWorkspaceFolder},target=/home/nodeop/bin/hyperdrive,type=bind,consistency=cached",
	"workspaceFolder": "/home/nodeop/bin/hyperdrive"
}
