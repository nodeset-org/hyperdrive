# Autogenerated - DO NOT MODIFY THIS FILE DIRECTLY 
# If you want to overwrite some of these values with your own customizations,
# please add them to `override/validator.yml`.
# 
# See https://docs.docker.com/compose/extends/#adding-and-overriding-configuration
# for more information on overriding specific parameters of docker-compose files.

version: "3.7"
services:
  sw-vc:
    image: {{.GetStakewiseVcContainerTag}}
    user: root
    container_name: {{.ProjectName}}-sw-vc
    restart: unless-stopped
    stop_grace_period: 3m
    {{$module_dir := (printf "%s/%s/%s" .UserDataPath.Value .Modules.ModulesDirectory .Modules.Stakewise.StakewiseDaemonRoute)}}
    volumes:
      - /usr/share/hyperdrive/scripts:/usr/share/hyperdrive/scripts:ro
      - {{$module_dir}}/{{.Modules.Stakewise.KeystoreDir}}:/validators
    networks:
      - net
    environment:
      - NETWORK={{.Network}}
      - EC_CLIENT={{if .IsLocalMode}}{{.LocalExecutionConfig.ExecutionClient}}{{else}}{{.ExternalExecutionConfig.ExecutionClient}}{{end}}
      - CC_CLIENT={{if .IsLocalMode}}{{.LocalBeaconConfig.BeaconNode}}{{else}}{{.ExternalBeaconConfig.BeaconNode}}{{end}}
      - CC_API_ENDPOINT={{.BnHttpUrl}}
      - CC_RPC_ENDPOINT={{.BnRpcUrl}}
      - FALLBACK_CC_API_ENDPOINT={{.FallbackBnHttpUrl}}
      - FALLBACK_CC_RPC_ENDPOINT={{.FallbackBnRpcUrl}}
      - FEE_RECIPIENT={{.StakewiseFeeRecipient}}
      - ENABLE_METRICS={{.Metrics.EnableMetrics}}
      - VC_METRICS_PORT={{.Modules.Stakewise.VcCommon.MetricsPort}}
      - DOPPELGANGER_DETECTION={{.Modules.Stakewise.VcCommon.DoppelgangerDetection}}
      - VC_ADDITIONAL_FLAGS={{.GetStakewiseVcAdditionalFlags}}
      - ENABLE_BITFLY_NODE_METRICS={{.Metrics.EnableBitflyNodeMetrics}}
      - BITFLY_NODE_METRICS_SECRET={{.Metrics.BitflyNodeMetrics.Secret}}
      - BITFLY_NODE_METRICS_ENDPOINT={{.Metrics.BitflyNodeMetrics.Endpoint}}
      - BITFLY_NODE_METRICS_MACHINE_NAME={{.Metrics.BitflyNodeMetrics.MachineName}}
      - GRAFFITI={{.StakewiseGraffiti}}
    entrypoint: sh
    command: "/usr/share/hyperdrive/scripts/{{.GetVcStartScript}}"
    cap_drop:
      - all
    cap_add:
      - dac_override
    security_opt:
      - no-new-privileges
networks:
  net:
