# Autogenerated - DO NOT MODIFY THIS FILE DIRECTLY 
# If you want to overwrite some of these values with your own customizations,
# please add them to `override/daemon.yml`.
# 
# See https://docs.docker.com/compose/extends/#adding-and-overriding-configuration
# for more information on overriding specific parameters of docker-compose files.

version: "3.7"
services:
  sw_operator:
    image: {{.Modules.Stakewise.OperatorContainerTag.Value}}
    user: root
    container_name: {{.ProjectName}}_sw_operator
    restart: unless-stopped
    {{$module_dir := (printf "%s/%s/%s" .UserDataPath.Value .Modules.ModulesDirectory .Modules.Stakewise.DaemonRoute)}}
    volumes:
      - {{$module_dir}}:{{$module_dir}}
    command:
      - "src/main.py"
      - "start"
      - "--data-dir={{$module_dir}}"
      - "--network={{.Network}}"
      - "--vault={{.StakewiseVault}}"
      - "--execution-endpoints="
      - "--consensus-endpoints="
      - "--hot-wallet-file={{$module_dir}}/{{.Modules.Stakewise.WalletFilename}}"
      - "--hot-wallet-password-file={{$module_dir}}/{{.Modules.Stakewise.PasswordFilename}}"
      - "--keystores-dir={{$module_dir}}/{{.Modules.ValidatorDir}}/{{.Modules.Stakewise.DaemonRoute}}"
      - "--keystores-password-file={$module_dir}}/{{.Modules.ValidatorDir}}/{{.Modules.Stakewise.DaemonRoute}}/{{.Modules.Stakewise.KeystorePasswordFile}}"
      - "--enable-metrics={{.Metrics.EnableMetrics}}
      - "--metrics-host=0.0.0.0"
      - "--max-fee-per-gas-gwei={{.AutoTxMaxFeeInt}}
    networks:
      - net
    environment:
      - 
    cap_drop:
      - all
    cap_add:
      - dac_override
    security_opt:
      - no-new-privileges
networks:
  net: